- В задании на SDF пропустили двойку в dMSE/dS, хотя это не влияет на сходимость.
- В задании edge sampling не пояснено что такое pi, раньше этого обозначения не было.
- В задании edge sampling утверждение, что первый интеграл в сумме в теореме Рейнольдса равен нулю неверно: если двигать вершину треугольника, то f(p, S) изменится во внутренних точках, то есть цвет в точке зависит не только от цвета текстуры как написано в тексте, но и от позиций вершин.
- edge sampling не сходится, если learning rate текстуры высокий, хорошо бы про это написать
- В тестовой сцене edge sampling вершины находятся прямо на границе картинки, и при оптимизации вылетают за границу, что очень плохо для оптимизации

